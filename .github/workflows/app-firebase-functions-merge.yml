# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy App to Firebase Functions on merge
'on':
  push:
    branches:
      - main
    paths:
      - .github/workflows/app-firebase-functions-merge.yml
      - packages/server/functions/**

jobs:
  app_build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        run: yarn --frozen-lockfile

      - name: Build and Deploy to Firebase Functions
        run: cd packages/server/functions && npx firebase-tools deploy --only functions --token ${{ secrets.FIREBASE_TOKEN }}
